[1mdiff --git a/src/ai/llm.js b/src/ai/llm.js[m
[1mindex cbe4e9e..94c4e55 100644[m
[1m--- a/src/ai/llm.js[m
[1m+++ b/src/ai/llm.js[m
[36m@@ -1,47 +1,44 @@[m
[31m-import { GoogleGenAI } from '@google/genai';[m
[31m-import dotenv from 'dotenv';[m
[32m+[m[32mimport OpenAI from "openai";[m
[32m+[m[32mimport dotenv from "dotenv";[m
 [m
 dotenv.config();[m
 [m
[31m-// Inisialisasi Google Gen AI menggunakan API Key dari file .env[m
[31m-const apiKey = process.env.API_KEY_AI;[m
[31m-const ai = apiKey ? new GoogleGenAI({ apiKey }) : null;[m
[32m+[m[32mconst apiKey = process.env.OPENAI_API_KEY;[m
[32m+[m
[32m+[m[32mconst client = apiKey[m
[32m+[m[32m  ? new OpenAI({ apiKey })[m
[32m+[m[32m  : null;[m
 [m
 /**[m
[31m- * Menghasilkan respons menggunakan Google Gemini AI asli[m
[31m- * @param {Array} history - Riwayat percakapan [{role: 'user'|'assistant', content: '...'}][m
[32m+[m[32m * Menghasilkan respons AI menggunakan OpenAI GPT-5[m
[32m+[m[32m * @param {Array} history[m
  */[m
 export async function generateAIResponse(history) {[m
[31m-  // Pengaman jika API Key tidak terpasang (Sangat bagus untuk keamanan Portofolio Publik)[m
[31m-  if (!ai || !apiKey || apiKey.includes('masukkan_api_key_ai')) {[m
[31m-    const lastMessage = history[history.length - 1].content;[m
[31m-    return `[Demo Mode] Bot menerima pesan: "${lastMessage}". (Konfigurasi API Key diperlukan untuk respons pintar).`;[m
[32m+[m[32m  if (!client || !apiKey) {[m
[32m+[m[32m    const lastMessage = history[history.length - 1]?.content || "";[m
[32m+[m
[32m+[m[32m    return `[Demo Mode] Bot menerima pesan: "${lastMessage}". (Konfigurasi OPENAI_API_KEY diperlukan.)`;[m
   }[m
 [m
   try {[m
[31m-    // 1. Format ulang riwayat chat agar sesuai dengan standar Google Gemini API[m
[31m-    const contents = history.map(chat => ({[m
[31m-      role: chat.role === 'assistant' ? 'model' : 'user',[m
[31m-      parts: [{ text: chat.content }][m
[31m-    }));[m
[31m-[m
[31m-    // 2. Berikan instruksi dasar (System Instruction) agar bot bertindak profesional[m
[31m-    const systemInstruction = "Anda adalah asisten virtual WhatsApp pintar yang ramah, profesional, dan membantu untuk operasional bisnis.";[m
[31m-[m
[31m-    // 3. Panggil model Gemini terbaru (gemini-2.5-flash) yang sangat cepat dan efisien[m
[31m-    const response = await ai.models.generateContent({[m
[31m-      model: 'gemini-2.5-flash',[m
[31m-      contents: contents,[m
[31m-      config: {[m
[31m-        systemInstruction: systemInstruction,[m
[31m-        temperature: 0.7, // Membuat respons terasa natural tapi tetap terarah[m
[31m-      }[m
[32m+[m[32m    const messages = [[m
[32m+[m[32m      {[m
[32m+[m[32m        role: "system",[m
[32m+[m[32m        content:[m
[32m+[m[32m          "Anda adalah asisten virtual WhatsApp yang ramah, profesional, membantu, dan menjawab dalam bahasa yang digunakan pengguna."[m
[32m+[m[32m      },[m
[32m+[m[32m      ...history[m
[32m+[m[32m    ];[m
[32m+[m
[32m+[m[32m    const response = await client.responses.create({[m
[32m+[m[32m      model: "gpt-5",[m
[32m+[m[32m      input: messages[m
     });[m
 [m
[31m-    return response.text || "Maaf, saya tidak menangkap maksud Anda.";[m
[32m+[m[32m    return response.output_text || "Maaf, saya tidak dapat menjawab saat ini.";[m
 [m
   } catch (error) {[m
[31m-    console.error('[Gemini AI Error]:', error);[m
[31m-    return "Maaf, sistem AI sedang mengalami gangguan teknis sejenak.";[m
[32m+[m[32m    console.error("[OpenAI Error]", error);[m
[32m+[m[32m    return "Maaf, sistem AI sedang mengalami gangguan.";[m
   }[m
 }[m
